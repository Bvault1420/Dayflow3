# Das ist die alte Kairos-App — nicht löschen, nicht umbauen

Diese Ordner ist eine **eingefrorene Kopie** der Version, die der Cloud-Agent gebaut hat (Branch `cursor/aippy-tiktok-roblox-app-36fa`).

- Hier nichts löschen und nichts „aufräumen“.
- Du darfst Dateien **lesen**, um zu verstehen, wie etwas funktioniert.
- Die neue App baust du **nicht** hier, sondern im Ordner `rebuild/`.

Die alte App weiter starten:

```bash
cd legacy
cp .env.example .env.local   # falls noch nicht vorhanden
npm install
npm run dev
```
